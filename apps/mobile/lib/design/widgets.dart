/// Moved to `components.dart`.
///
/// Re-exported rather than deleted so an import of this path keeps compiling.
/// The design-system widgets now live in one file alongside the tokens they
/// implement, and each carries the Tailwind classes it was translated from.
library;

export 'components.dart';
