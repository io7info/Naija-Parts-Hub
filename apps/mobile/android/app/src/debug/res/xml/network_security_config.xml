<?xml version="1.0" encoding="utf-8"?>
<!--
  DEBUG BUILDS ONLY.

  Android 9 (API 28) blocks cleartext HTTP by default. The Firebase Local
  Emulator Suite serves plain HTTP, so without this the dealer app cannot reach
  it and every call fails with:

      [firebase_auth/unknown] An internal error has occurred.
      [ Cleartext HTTP traffic to 10.0.2.2 not permitted ]

  Scoped deliberately:
    * only these hosts, not a blanket usesCleartextTraffic="true"
    * only src/debug, so release builds keep the secure-by-default posture
      and no exception ever ships to the Play Store

  10.0.2.2  — the Android emulator's alias for the host machine's loopback
  10.0.3.2  — the same thing on Genymotion
  localhost / 127.0.0.1 — a physical device port-forwarded with `adb reverse`
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">10.0.3.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
