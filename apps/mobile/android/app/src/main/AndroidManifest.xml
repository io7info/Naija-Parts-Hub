<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:label="Naija Parts Hub"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!--
            Firebase must be initialised from Dart, not automatically at process
            start.

            FirebaseInitProvider is a ContentProvider the SDK merges in, and it
            builds the [DEFAULT] app from google-services.json before any Dart
            code runs — always against the LIVE project. Emulator mode then
            calls initializeApp with demo-naija-parts-hub options and fails with

              [duplicate-app] A Firebase App named "[DEFAULT]" already exists

            so the dealer app could not talk to the Emulator Suite at all once
            `flutterfire configure` added that file. Deleting the app from Dart
            does not help: firebase_core reports no apps at that point, because
            the auto-created one is only visible after the first initializeApp.

            Removing the provider makes lib/core/firebase_bootstrap.dart the one
            place that decides which project the app talks to, in both modes.

            Cost: Crashlytics and Sessions now start when Dart initialises
            Firebase rather than at process start, so a native crash in the
            milliseconds before that is not reported. Worth it — the alternative
            is a build that silently reads production data whenever an emulator
            wiring call is missed.
        -->
        <provider
            android:name="com.google.firebase.provider.FirebaseInitProvider"
            android:authorities="${applicationId}.firebaseinitprovider"
            android:exported="false"
            tools:node="remove" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
